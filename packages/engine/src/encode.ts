/**
 * OWNER: DEV A — ROADMAP-A.md Task 3.1.
 * Export via mediabunny Output: Mp4/WebMOutputFormat + BufferTarget,
 * CanvasSource(codec avc/vp9) per rendered frame, AudioBufferSource(aac/opus)
 * from getAudioBuffer, "export-progress" CustomEvent(detail: 0..1) per second of footage.
 * Signature: exportProject(project, opts): Promise<Blob>
 */
export {};
