/**
 * OWNER: DEV B — ROADMAP-B.md Task 2.1 (TDD).
 * Required API:
 *   hitTestFocal(px, py, zp, frameW): boolean            // 24px grab radius
 *   normalizeClick(clientX, clientY, rect, frame): {x,y} // clamped 0-1, FRAME-relative
 */
export {};
