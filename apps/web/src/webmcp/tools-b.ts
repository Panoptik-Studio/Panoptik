/**
 * OWNER: DEV B — ROADMAP-B.md Task 5.2. Register via A's registerToolWithLifecycle ONLY.
 * Tools to implement:
 *   propose_zoom_points · add_text_overlay · set_background · generate_captions
 *   commit_staged_changes (showConfirmDialog-gated with full diff)
 */
import { registerToolWithLifecycle } from "./lifecycle";

void registerToolWithLifecycle; // remove once tools are registered

export function registerEditingTools(): void {}
