/**
 * OWNER: DEV A — ROADMAP-A.md Task 5.2. Register via registerToolWithLifecycle ONLY.
 * Tools to implement:
 *   get_project_state (readOnlyHint) · list_scenes (readOnlyHint)
 *   get_click_log (readOnlyHint)     · export_clip (showConfirmDialog-gated)
 */
import { registerToolWithLifecycle } from "./lifecycle";

void registerToolWithLifecycle; // remove once tools are registered

export function registerEngineTools(): void {}
