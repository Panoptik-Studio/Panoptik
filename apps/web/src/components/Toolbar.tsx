/**
 * OWNER: DEV A — ROADMAP-A.md Task 2.4.
 * Import button, Record button (opens RecordModal), play/pause + time display +
 * undo/redo buttons (consume useProjectStore; never edit B's store files).
 */
export function Toolbar() {
  return null;
}
