/**
 * OWNER: DEV A — ROADMAP-A.md Tasks 3.4/3.5.
 * Zoom panel (depth/duration/easing/delete on selectedZoomId — a field in B's
 * store; consume, never edit the store file), background picker, text overlays,
 * aspect preset select.
 */
export function Inspector() {
  return null;
}
