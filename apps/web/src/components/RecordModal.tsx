/**
 * OWNER: DEV B — ROADMAP-B.md Task 3.3.
 * Recording flow UI for YOUR capture slice (record.ts): source-picker explainer,
 * webcam preview tile, Start/Stop → engine.loadRecording(blobs) → setProject.
 * Opens via window CustomEvent "open-record-modal" dispatched by A's Toolbar —
 * event coupling keeps file ownership clean.
 */
export function RecordModal() {
  return null;
}
